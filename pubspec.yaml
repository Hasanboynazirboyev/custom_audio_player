name: custom_audio_player
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.3.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.6
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^3.0.0

flutter:
  plugin:
    platforms:
      android:
        package: uz.xasanboy.custom_audio_player
        pluginClass: CustomAudioPlayerPlugin
      ios:
        pluginClass: CustomAudioPlayerPlugin

